{
  "name": "bnpl-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Deliberately-vulnerable BNPL demo — TypeScript monorepo (React frontend + Node/Express backend). Training/security target.",
  "license": "UNLICENSED",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "start": "npm run start --workspace=backend",
    "start:secure": "npm run start:secure --workspace=backend",
    "selftest": "npm run selftest --workspace=backend"
  }
}
